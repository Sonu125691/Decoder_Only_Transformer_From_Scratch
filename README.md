# Decoder_Only_Transformer_From_Scratch
An educational implementation of a decoder-only Transformer (GPT-style) built fully from scratch using PyTorch, trained on 20 classic thriller novels to study text generation and the internal workflow of GPT models.
